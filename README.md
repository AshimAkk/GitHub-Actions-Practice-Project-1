# GitHub-Actions-Practice-Project-1

Tasks:

- Create a workflow that installs npm dependencies and runs lint,test and build.
- Use context expressions to display the github object using the issues event trigger.
